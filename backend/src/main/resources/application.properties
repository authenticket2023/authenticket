spring.config.import=secrets.properties
authenticket.api-version=1

server.error.include-binding-errors=always
server.error.include-message=always

## Connect to the database
spring.datasource.url=jdbc:postgresql://authenticket.cu48jvtdlxqw.ap-southeast-1.rds.amazonaws.com:5432/authenticket

## JPA Config
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.properties.hibernate.default_schema=dev

spring.mail.host = smtp.office365.com
spring.mail.port = 587
spring.mail.properties.mail.smtp.starttls.enable = true

#spring.config.name=secrets.properties
#spring.config.location=classpath:/,file:/app/
## Secrets - Read from environment variables

# Spring Boot Application Properties

# Database Configuration
#spring.datasource.url=${SPRING_DATASOURCE_URL}
#spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
#spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}
#
## JPA Configuration
#spring.jpa.hibernate.ddl-auto=${SPRING_JPA_HIBERNATE_DDL_AUTO}
#spring.jpa.show-sql=${SPRING_JPA_SHOW_SQL}
#spring.jpa.properties.hibernate.dialect=${SPRING_JPA_PROPERTIES_HIBERNATE_DIALECT}
#spring.jpa.properties.hibernate.format_sql=${SPRING_JPA_PROPERTIES_HIBERNATE_FORMAT_SQL}
#spring.jpa.properties.hibernate.default_schema=${SPRING_JPA_PROPERTIES_HIBERNATE_DEFAULT_SCHEMA}
#
## Mail Configuration
#spring.mail.host=${SPRING_MAIL_HOST}
#spring.mail.port=${SPRING_MAIL_PORT}
#spring.mail.properties.mail.smtp.starttls.enable=${SPRING_MAIL_PROPERTIES_MAIL_SMTP_STARTTLS_ENABLE}
#
## Authenticket Configuration
#authenticket.secret-key=${AUTHENTICKET_SECRET_KEY}
#authenticket.database-id=${AUTHENTICKET_DATABASE_ID}
#authenticket.frontend-dev-url=${AUTHENTICKET_FRONTEND_DEV_URL}
#authenticket.frontend-production-url=${AUTHENTICKET_FRONTEND_PRODUCTION_URL}
#authenticket.backend-dev-url=${AUTHENTICKET_BACKEND_DEV_URL}
#authenticket.backend-production-url=${AUTHENTICKET_BACKEND_PRODUCTION_URL}
#authenticket.loadbalancer-url=${AUTHENTICKET_LOADBALANCER_URL}
#authenticket.S3-bucket-name=${AUTHENTICKET_S3_BUCKET_NAME}
#authenticket.smtp-username=${AUTHENTICKET_SMTP_USERNAME}
#authenticket.smtp-password=${AUTHENTICKET_SMTP_PASSWORD}
#
## Cloud AWS Configuration
#cloud.aws.credentials.access-key=${CLOUD_AWS_CREDENTIALS_ACCESS_KEY}
#cloud.aws.credentials.secret-key=${CLOUD_AWS_CREDENTIALS_SECRET_KEY}
#cloud.aws.region.static=${CLOUD_AWS_REGION_STATIC}
