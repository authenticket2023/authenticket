import React, { useEffect, useState } from 'react';
import { Navigate } from 'react-router-dom';
import { NavbarAdmin } from '../../Navbar';

export const EventAdmin = () => {
    useEffect(() => {
    }, []);

    return (
        <div>
            < NavbarAdmin />
            <h1>
                EventAdmin Page under construction ...
            </h1>
        </div>

    )
}